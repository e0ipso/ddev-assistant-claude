#ddev-generated
# Mount host Claude Code config read-only into the seed directory. The post-start
# hook mirrors it into the writable ~/.claude runtime directory on every start.
services:
  web:
    volumes:
      - "${HOME}/.claude:${HOME}/.cred-seed/claude:ro"
