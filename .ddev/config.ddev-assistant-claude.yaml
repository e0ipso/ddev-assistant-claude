#ddev-generated
# Claude Code addon configuration
# This file is generated by the ddev-assistant-claude addon and should not be manually edited

web_environment:
  - CLAUDE_CONFIG_DIR=$HOME/.claude
  - AI_TASK_MANAGER_ASSISTANT=claude

hooks:
  post-start:
    - exec: npm config set prefix /usr/local && npm install -g @anthropic-ai/claude-code 2>&1 || echo "Note: Claude Code npm install may have completed with npm warnings"

override_config: false
